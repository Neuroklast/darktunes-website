/**
 * Sentinel UUID for the default single-organization tenant (darkTunes).
 * Mirrors DEFAULT_LABEL_ID on api_credentials — same UUID for transitional compatibility.
 */
export const DEFAULT_ORGANIZATION_ID = '00000000-0000-0000-0000-000000000000'

export const DEFAULT_ORGANIZATION_SLUG = 'darktunes'