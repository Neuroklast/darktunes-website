# =============================================================================
# .env.example — darkTunes Music Group
# =============================================================================
# Copy this file to .env.local and fill in your real values.
# NEVER commit .env.local or any file containing real secrets.
#
# Variable prefixes:
#   VITE_*          → exposed to the browser (Vite build-time injection)
#   (no prefix)     → server-side only (Vercel Edge / Supabase Edge Functions)
#
# See DEPLOYMENT.md for a full description of each variable and where to
# obtain the values.
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase  (client-side — required)
# https://supabase.com → Project Settings → API
# -----------------------------------------------------------------------------
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key-here

# -----------------------------------------------------------------------------
# Supabase  (server-side — used in Vercel Serverless Functions only)
# https://supabase.com → Project Settings → API → service_role key (keep secret!)
# -----------------------------------------------------------------------------
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key-here

# -----------------------------------------------------------------------------
# Cloudflare R2  (server-side — used in Vercel / Supabase Edge Functions only)
# https://dash.cloudflare.com → R2 → Manage R2 API Tokens
# -----------------------------------------------------------------------------
CLOUDFLARE_R2_ACCOUNT_ID=your-cloudflare-account-id-here
CLOUDFLARE_R2_ACCESS_KEY_ID=your-r2-access-key-id-here
CLOUDFLARE_R2_SECRET_ACCESS_KEY=your-r2-secret-access-key-here
CLOUDFLARE_R2_BUCKET_NAME=darktunes-assets
CLOUDFLARE_R2_PUBLIC_URL=https://cdn.your-domain.com

# -----------------------------------------------------------------------------
# Runtime / deployment (set automatically by Vercel — no manual action needed)
# -----------------------------------------------------------------------------
# VERCEL_ENV=production   # production | preview | development  (Vercel-injected)
# NODE_ENV=production     # production | development            (Vite / Node-injected)

# -----------------------------------------------------------------------------
# Spotify Web API  (server-side — used in api/sync-artist.ts)
# https://developer.spotify.com/dashboard → create an app → Client Credentials
# Generate a token via: POST https://accounts.spotify.com/api/token
# (client_credentials grant; must be refreshed periodically)
# -----------------------------------------------------------------------------
SPOTIFY_ACCESS_TOKEN=your-spotify-client-credentials-access-token

# -----------------------------------------------------------------------------
# Songkick API  (server-side — used in api/sync-artist.ts)
# https://www.songkick.com/developer → request an API key
# -----------------------------------------------------------------------------
SONGKICK_API_KEY=your-songkick-api-key-here

# -----------------------------------------------------------------------------
# Discogs API  (server-side — reserved for future sync integration)
# https://www.discogs.com/settings/developers → generate a personal access token
# -----------------------------------------------------------------------------
DISCOGS_TOKEN=your-discogs-personal-access-token
